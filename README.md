# 🐍 Snake Game - 贪吃蛇游戏

基于 Qt 框架开发的经典贪吃蛇游戏，采用现代 C++17 标准，具有清晰的前后端分离架构。

![Version](https://img.shields.io/badge/version-1.0.0-blue)
![Qt](https://img.shields.io/badge/Qt-5.15%2B%20%7C%206.x-green)
![C++](https://img.shields.io/badge/C%2B%2B-17-orange)

## ✨ 特性

- 🎮 经典贪吃蛇玩法
- 🎨 现代深色主题 UI
- 🖌️ 支持 QPainter 和 QGraphicsScene 双渲染模式
- ⌨️ 支持方向键和 WASD 双操作模式
- 📊 实时分数显示
- ⏸️ 游戏暂停/继续功能
- 🔄 游戏结束后快速重新开始

## 📁 项目结构

```
Snake/
├── CMakeLists.txt           # CMake 构建配置
├── README.md                # 项目说明文档
└── src/
    ├── main.cpp             # 程序入口
    ├── Constants/           # 常量定义
    │   ├── Constants.h      # 游戏参数常量
    │   ├── Direction.h      # 方向枚举
    │   └── GameState.h      # 游戏状态枚举
    ├── core/                # 核心逻辑层（后端）
    │   ├── Snake.h/cpp      # 蛇类
    │   ├── Food.h/cpp       # 食物类
    │   └── GameLogic.h/cpp  # 游戏逻辑控制器
    └── ui/                  # 界面层（前端）
        ├── MainWindow.h/cpp # 主窗口
        └── GameWidget.h/cpp # 游戏渲染组件
```

## 🛠️ 环境要求

- **操作系统**: Windows / Linux / macOS
- **编译器**: 支持 C++17 的编译器 (MSVC 2019+, GCC 8+, Clang 8+)
- **Qt 版本**: Qt 5.15+ 或 Qt 6.x
- **构建工具**: CMake 3.16+

## 🚀 构建与运行

### Windows (推荐使用 Qt Creator)

1. **使用 Qt Creator 打开项目**
   - 打开 Qt Creator
   - 选择 `文件` → `打开文件或项目`
   - 选择 `CMakeLists.txt` 文件
   - 配置 Kit 并点击 `Configure Project`

2. **构建并运行**
   - 点击左下角的 `运行` 按钮 (▶️)

### 命令行构建

```bash
# 1. 创建构建目录
mkdir build && cd build

# 2. 配置项目（确保 Qt 在 PATH 中或指定 Qt 路径）
cmake .. -DCMAKE_PREFIX_PATH="C:/Qt/6.x.x/msvc2019_64"

# 3. 编译
cmake --build . --config Release

# 4. 运行
./SnakeGame        # Linux/macOS
.\Release\SnakeGame.exe  # Windows
```

### 高级启动选项

支持通过命令行参数切换渲染后端：

```bash
# 使用 QGraphicsScene 渲染（推荐）
.\SnakeGame.exe --renderer=scene

# 使用 QPainter 下层渲染（默认）
.\SnakeGame.exe --renderer=widget
```

### Linux

```bash
# 安装依赖 (Ubuntu/Debian)
sudo apt install qt6-base-dev cmake g++

# 构建
mkdir build && cd build
cmake ..
make -j$(nproc)

# 运行
./SnakeGame
```

## 🎮 操作说明

| 操作      | 按键             |
| --------- | ---------------- |
| 向上移动  | `↑` 或 `W`       |
| 向下移动  | `↓` 或 `S`       |
| 向左移动  | `←` 或 `A`       |
| 向右移动  | `→` 或 `D`       |
| 开始/重玩 | `空格` 或 `回车` |
| 暂停/继续 | `P` 或 `ESC`     |

## ⚙️ 游戏参数配置

可在 `src/Constants/Constants.h` 中调整游戏参数：

```cpp
constexpr int kDefaultBoardWidth = 20;   // 游戏区域宽度（格数）
constexpr int kDefaultBoardHeight = 15;  // 游戏区域高度（格数）
constexpr int kInitialSnakeLength = 3;   // 初始蛇长度
constexpr int kGameTickInterval = 200;   // 游戏速度（毫秒，越小越快）
constexpr int kScorePerFood = 10;        // 每个食物得分
constexpr int kCellSize = 30;            // 单元格像素大小
```

## 🏗️ 架构设计

本项目采用**前后端分离**架构：

```
┌─────────────────────────────────────────────────────┐
│                    前端 (UI Layer)                  │
│                                                     │
│      ┌─────────────────┐   ┌───────────────────┐    │
│      │   GameWidget    │   │   SceneGameView   │    │
│      │  (QPainter)     │   │ (QGraphicsScene)  │    │
│      └────────▲────────┘   └─────────▲─────────┘    │
│               │                      │              │
│               └──────────┐           │              │
│                          │           │              │
│                  ┌───────┴───────────┴──────┐       │
│                  │       MainWindow         │       │
│                  │ (主窗口/渲染器选择)      │       │
│                  └───────────▲──────────────┘       │
│                              │                      │
│                              │ Signals/Slots        │
│                              ▼                      │
├─────────────────────────────────────────────────────┤
│                   后端 (Core Layer)                 │
│  ┌─────────────────────────────────────────────┐   │
│  │              GameLogic (游戏控制器)          │   │
│  │  ┌─────────────┐      ┌─────────────┐       │   │
│  │  │    Snake    │      │    Food     │       │   │
│  │  │   (蛇逻辑)  │      │  (食物逻辑) │       │   │
│  │  └─────────────┘      └─────────────┘       │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

## 📄 License

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
